name: antigravity-validate

on:
  pull_request:
    branches: ["main"]
  merge_group:
    types: [checks_requested]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: antigravity-validate-${{ github.event.pull_request.number || github.event.merge_group.head_sha || github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

env:
  # Garante que JavaScript actions rodem no runtime Node 24.
  # O runtime do projeto continua controlado em actions/setup-node.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  validate:
    name: Required validation
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    env:
      CI: true

      VITE_SUPABASE_URL: ${{ vars.CI_SUPABASE_URL || 'https://example.supabase.co' }}

      # Contrato público atual do frontend.
      # CI_SUPABASE_ANON_KEY permanece apenas como fallback transitório.
      VITE_SUPABASE_PUBLISHABLE_KEY: ${{ vars.CI_SUPABASE_PUBLISHABLE_KEY || vars.CI_SUPABASE_ANON_KEY || 'example-publishable-key' }}

      VITE_SUPABASE_FUNCTIONS_URL: ${{ vars.CI_SUPABASE_FUNCTIONS_URL || 'https://example.supabase.co/functions/v1' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      # -----------------------------------------------------------------------
      # Classificação de risco
      # -----------------------------------------------------------------------

      - name: Detect validation scope
        id: scope
        run: |
          set -euo pipefail

          : > /tmp/pr-files.txt
          : > /tmp/delta-files.txt

          pr_known=false
          delta_known=false
          final_gate=false

          if [[ "$GITHUB_EVENT_NAME" == "merge_group" || "$GITHUB_EVENT_NAME" == "workflow_dispatch" ]]; then
            final_gate=true
          elif [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            event_action="$(jq -r '.action // empty' "$GITHUB_EVENT_PATH")"
            base_sha="$(jq -r '.pull_request.base.sha // empty' "$GITHUB_EVENT_PATH")"
            head_sha="$(jq -r '.pull_request.head.sha // empty' "$GITHUB_EVENT_PATH")"

            if [[ -n "$base_sha" && -n "$head_sha" ]] &&
               git cat-file -e "${base_sha}^{commit}" 2>/dev/null &&
               git cat-file -e "${head_sha}^{commit}" 2>/dev/null; then
              git diff --name-only "$base_sha...$head_sha" > /tmp/pr-files.txt
              pr_known=true
            fi

            if [[ "$event_action" == "synchronize" ]]; then
              before_sha="$(jq -r '.before // empty' "$GITHUB_EVENT_PATH")"
              after_sha="$(jq -r '.after // empty' "$GITHUB_EVENT_PATH")"

              if [[ -n "$before_sha" && -n "$after_sha" && "$after_sha" == "$head_sha" ]] &&
                 git cat-file -e "${before_sha}^{commit}" 2>/dev/null &&
                 git cat-file -e "${after_sha}^{commit}" 2>/dev/null; then
                git diff --name-only "$before_sha" "$after_sha" > /tmp/delta-files.txt
                delta_known=true
              fi
            fi
          fi

          node scripts/ci/classify-validation-scope.mjs classify \
            --pr-files /tmp/pr-files.txt \
            --pr-known "$pr_known" \
            --delta-files /tmp/delta-files.txt \
            --delta-known "$delta_known" \
            --final-gate "$final_gate" | tee -a "$GITHUB_OUTPUT"

          echo
          echo "PR files:"
          sed 's/^/pr: /' /tmp/pr-files.txt
          echo "Delta files:"
          sed 's/^/delta: /' /tmp/delta-files.txt

      # -----------------------------------------------------------------------
      # Setup comum
      # -----------------------------------------------------------------------

      - name: Setup pnpm
        uses: pnpm/setup@v2
        with:
          install: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Install ripgrep
        run: |
          sudo apt-get update
          sudo apt-get install -y ripgrep
          rg --version

      # -----------------------------------------------------------------------
      # Supabase — somente para alterações de alto risco
      # -----------------------------------------------------------------------

      - name: Setup Supabase CLI
        if: steps.scope.outputs.needs_supabase == 'true'
        uses: supabase/setup-cli@v3
        with:
          # Mantido fixo para não misturar mudança de estratégia do CI
          # com upgrade de runtime/CLI.
          version: 2.72.7

      - name: Start disposable Supabase
        if: steps.scope.outputs.needs_supabase == 'true'
        run: |
          set -euo pipefail

          cli_state="$RUNNER_TEMP/rebanhosync-supabase-cli-latest"

          if [[ -f supabase/.temp/cli-latest ]]; then
            cp supabase/.temp/cli-latest "$cli_state"
          else
            rm -f "$cli_state"
          fi

          supabase init --force
          supabase start
          supabase db reset --yes

      - name: Export local Supabase test environment
        if: steps.scope.outputs.needs_supabase == 'true'
        run: |
          set -euo pipefail

          env_file="$RUNNER_TEMP/rebanhosync-supabase.env"

          supabase status -o env > "$env_file"

          set -a
          source "$env_file"
          set +a

          {
            echo "REBANHOSYNC_TEST_DB_URL=$DB_URL"
            echo "REBANHOSYNC_TEST_API_URL=$API_URL"
            echo "REBANHOSYNC_TEST_SERVICE_ROLE_KEY=$SERVICE_ROLE_KEY"
          } >> "$GITHUB_ENV"

      - name: Prepare commercial concurrency fixture
        if: steps.scope.outputs.needs_supabase == 'true'
        run: node scripts/ci/prepare-commercial-concurrency-fixture.mjs

      # -----------------------------------------------------------------------
      # Validação de aplicação
      #
      # Não roda para PR exclusivamente documental.
      # -----------------------------------------------------------------------

      - name: Lint
        if: steps.scope.outputs.needs_app == 'true'
        run: pnpm run lint

      - name: Fallow audit
        if: steps.scope.outputs.needs_app == 'true'
        run: pnpm exec fallow audit --gate new-only

      - name: Test
        if: steps.scope.outputs.needs_app == 'true'
        timeout-minutes: 20
        run: |
          set -euo pipefail

          vitest_args=(--bail=1 --maxWorkers=50% --testTimeout=15000 --reporter=verbose)

          if [[ "${{ steps.scope.outputs.run_global_tests }}" == "true" ]]; then
            pnpm exec vitest run "${vitest_args[@]}"
          else
            mapfile -t test_targets < <(node scripts/ci/classify-validation-scope.mjs test-targets /tmp/delta-files.txt)
            if (( ${#test_targets[@]} == 0 )); then
              echo "No safe incremental test target found; running the global suite."
              pnpm exec vitest run "${vitest_args[@]}"
            else
              printf 'Incremental test target: %s\n' "${test_targets[@]}"
              pnpm exec vitest run "${test_targets[@]}" "${vitest_args[@]}"
            fi
          fi

      - name: Build
        if: steps.scope.outputs.needs_build == 'true'
        run: pnpm run build

      # -----------------------------------------------------------------------
      # Cleanup Supabase
      #
      # Precisa executar mesmo quando algum passo anterior falhar.
      # -----------------------------------------------------------------------

      - name: Stop disposable Supabase
        if: always() && steps.scope.outputs.needs_supabase == 'true'
        run: |
          set +e

          supabase stop --no-backup
          rm -f "$RUNNER_TEMP/rebanhosync-supabase.env"

          git restore --source=HEAD --worktree -- \
            supabase/config.toml \
            supabase/.gitignore

          cli_state="$RUNNER_TEMP/rebanhosync-supabase-cli-latest"

          if [[ -f "$cli_state" ]]; then
            cp "$cli_state" supabase/.temp/cli-latest
          fi

          rm -f "$cli_state"

      # -----------------------------------------------------------------------
      # Gates obrigatórios para qualquer PR
      # -----------------------------------------------------------------------

      - name: Diff integrity
        if: always()
        run: git diff --check

      - name: Active document gates
        if: always()
        run: pnpm run gates:docs

      - name: Validation summary
        if: always()
        run: |
          echo "Validation scope"
          echo "----------------"
          echo "pr_scope=${{ steps.scope.outputs.pr_scope }}"
          echo "delta_scope=${{ steps.scope.outputs.delta_scope }}"
          echo "needs_app=${{ steps.scope.outputs.needs_app }}"
          echo "needs_supabase=${{ steps.scope.outputs.needs_supabase }}"
          echo "needs_build=${{ steps.scope.outputs.needs_build }}"
          echo "run_global_tests=${{ steps.scope.outputs.run_global_tests }}"
          echo "full_final_required=${{ steps.scope.outputs.full_final_required }}"
          echo "final_gate=${{ steps.scope.outputs.final_gate }}"

      - name: Repository must remain clean
        if: always()
        run: |
          set -euo pipefail

          echo "Git status:"
          git status -sb

          if [[ -n "$(git status --porcelain=v1)" ]]; then
            echo "Repository is dirty after validation." >&2
            echo "Generated or modified files must be committed or explicitly restored." >&2
            git status --porcelain=v1
            exit 1
          fi
